# ═══════════════════════════════════════════════
# EchoLink Python Backend — Dependencies
# ═══════════════════════════════════════════════
# Install: pip install -r requirements.txt
# Diagnose: python diagnose.py
# ═══════════════════════════════════════════════

# ── Server ──
fastapi==0.115.12
uvicorn[standard]==0.34.2
websockets==15.0.1

# ── Camera & Video ──
opencv-python-headless==4.11.0.86
# Pin numpy < 2.0 to avoid compatibility issues with mediapipe/tf2onnx
numpy>=1.24,<2.0

# ── Audio ──
sounddevice==0.5.1

# ── ML Inference ──
# Intel OpenVINO Runtime — primary inference engine for Models 1 & 2
# Provides Intel CPU optimizations and model caching for improved performance
openvino>=2024.0.0

# ONNX Runtime — fallback inference engine for Model 3
# Required for Model 3 due to Loop operator incompatibility with OpenVINO
onnxruntime>=1.22.0
# For GPU: pip install onnxruntime-gpu instead

# ── Virtual Camera ──
# Requires OBS Studio installed for the virtual camera driver
pyvirtualcam==0.12.0

# ── Text-to-Speech ──
# Primary: Intel OPEA SpeechT5 (OpenVINO-accelerated, natural voice)
transformers>=4.30.0
sentencepiece>=0.1.99
torch>=2.0.0
datasets>=2.14.0
# OpenVINO IR acceleration for SpeechT5 (optional but recommended).
# Enables running the converted OpenVINO IR (models/tts/speecht5_openvino/)
# produced by convert_tts_model.py. Without it, OPEA TTS uses the PyTorch path.
optimum-intel[openvino]>=1.21.0
# Optional: OpenVINO Runtime for hardware acceleration (Intel CPUs/GPUs)
# Install for 30-40% faster synthesis: pip install openvino>=2023.0.0
# openvino>=2023.0.0
# Secondary: Piper TTS (natural voice, requires model files)
piper-tts
# Fallback: pyttsx3 (uses Windows SAPI / system voice, no models needed)
pyttsx3==2.98

# ── Landmark Detection ──
# MediaPipe for hand landmark extraction
# If 0.10.14 doesn't work on your system, try just: pip install mediapipe
mediapipe==0.10.14

# ── Optional: Model Conversion ──
# Only needed if you want to convert .h5/.keras models locally.
# We recommend using Kaggle instead (see kaggle_convert_to_onnx.py)
# tensorflow>=2.16
# tf2onnx>=1.16.1
# keras>=3.0
